---
# Validate the whole nginx config before reloading, so a bad include can never
# take down the shared nginx (which also serves uMap). Fails loudly if invalid.
- name: Reload nginx
  ansible.builtin.shell: nginx -t && systemctl reload nginx
